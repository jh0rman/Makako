// Network layer — executes HTTP requests asynchronously.
// Planned dependencies: `reqwest` (async), `tokio`.
// Supported methods (v1): GET, POST, PUT, DELETE.
// TODO: implement `HttpClient` and `RequestPayload` / `ResponsePayload` types.
