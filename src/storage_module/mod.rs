// Storage layer — persists request collections to the local filesystem.
// Collections live in plain text files (similar to Bruno's `.bru` format).
// No cloud sync, no remote database.
// TODO: implement `Collection`, `RequestEntry`, and file read/write helpers.
